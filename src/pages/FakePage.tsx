import Layout from "../Layout/Layout";
import { Typography } from "@mui/material";

const FakePage = () => {
  return (
    <Layout>
      <Typography variant="h1">Fake Page</Typography>
    </Layout>
  );
};

export default FakePage;
